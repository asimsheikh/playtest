[project]
name = "playtest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.1",
    "pytest>=8.4.1",
    "pytest-playwright>=0.7.0",
]
